CREATE TABLE item (
                             id serial PRIMARY KEY ,
                             itemID INTEGER,
                             label text NOT NULL,
                             checked boolean
);
